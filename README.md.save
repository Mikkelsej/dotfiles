# Download Apps
sudo pacman -S yay discord libinput xorg-xinput wmctrl xdotool
yay -S code spotify libinput-gestures

# Add user to input group
sudo gpasswd -a $USER input

# Enable libgestures

sudo systemctl enable libinput-gestures.service
sudo systemctl start libinput-gestures.service

# Remove dotfiles
rm -rf Dotfiles/

# Clone
git clone https://github.com/Mikkelsej/dotfiles.git


